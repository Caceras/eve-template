// Bump on every product release; docs point here instead of repeating it.
// /api/health reports it, and an open tab or installed app compares it with
// the release its bundle was built from to reload into a newer one
// (lib/pwa/version-recovery.ts).
export const RELEASE = "voice-2026-09-26";
