---
"eve": patch
---

Create conversation sessions before their first turn through the eve HTTP channel, TypeScript client, and frontend `prewarm()` helpers so applications can move durable session startup off the first-message path.
