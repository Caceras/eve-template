---
"eve": patch
---

Export `auto()` from `eve/tools/agent-router/auto` for authored routing steps that want standard JEV selection while controlling candidate agents, instructions, or a live evaluation model. `agentRouter()` now accepts optional model-ID and instruction overrides or a custom authored selector step.
